# Never automatically apply breaking changes during audit.
# Use: npm audit fix        (safe, non-breaking only)
# DO NOT USE: npm audit fix --force  (downgrades NestJS/exceljs to incompatible versions)
audit-level=high
save-exact=false
